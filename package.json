{
  "name": "loop-health-slackbot",
  "version": "1.0.0",
  "description": "Slack bot that detects sick-related messages and replies with a Loop Health consultation link",
  "main": "src/server.js",
  "scripts": {
    "start": "node src/server.js",
    "dev": "node --watch src/server.js"
  },
  "dependencies": {
    "@slack/bolt": "^4.1.0",
    "better-sqlite3": "^11.7.0",
    "dotenv": "^16.4.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
